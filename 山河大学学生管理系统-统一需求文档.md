# 山河大学学生管理系统统一需求文档

> 版本：2026-07-29 · 当前阶段：前端视觉与多智能体基础能力

## 1. 产品定位

山河大学学生管理系统是面向学生、教师、校园系统管理员及校务工作人员的统一数字校园工作台。登录不区分学生和职员入口，系统依据账号身份自动进入对应工作台。学校名称暂定为“山河大学”，品牌主题为“山河无恙”，校训为“崇山仰止 · 纳川致远”。本阶段优先完成统一登录体验、角色工作台、多智能体基础能力、知识检索和课程编排入口。

## 2. 角色与权限

| 角色 | 主要职责 | 数据范围 |
| --- | --- | --- |
| 学生 | 查看本人课表、选课、成绩、成长建议，使用知识检索智能体 | 仅本人业务数据；知识库只读 |
| 教师 | 面向学校和学院承担教学任务，发布/维护课程与教学班，录入成绩 | 本人负责的课程、教学班及授权教学数据 |
| 校园系统管理员 | 浏览系统功能，接收学生和教师反馈，整理后转交 IT 维护人员 | 全校系统运营数据和问题工单 |
| 教务管理员 | 维护学院、专业、课程、学期、教学班和成绩审核 | 全校教务数据 |
| 学院管理员 | 维护本学院学生和培养方案 | 本学院数据 |
| 学生事务老师/辅导员 | 处理请假、资助、奖惩、投诉和风险预警 | 依据岗位和学生负责范围 |
| 档案管理员/教职工 | 管理授权范围内的电子档案或校园信息 | 岗位授权范围 |

权限由服务端令牌和角色依赖强制执行，客户端不传入学生编号、教师编号等主体字段来扩大查询范围。

## 3. 前端与品牌

1. 登录页使用 `新建文件夹` 中的校园航拍照片作为适度虚化的全屏背景，登录卡片保持清晰。
2. 校徽使用山峰、水纹和校名组成的 `img/shanhe-crest.svg`，同时出现在登录页和工作台侧栏。
3. 统一使用“山河大学”“山河智能中枢”“崇山仰止 · 纳川致远”等品牌文案。
4. 工作台名称为“工作台”，不再显示“角色工作台”。
5. 客户端导航不展示接口文档；接口仅作为开发、测试和运维边界使用。

## 4. 多智能体系统

### 4.1 主智能体

“山河主智能体”负责会话上下文、意图识别、任务路由、结果聚合和来源展示。统一接口为 `POST /api/multi-agent/chat`，响应包含主智能体、已调用子智能体、来源、业务数据和处理状态。

### 4.2 子智能体

| 子智能体 | 当前职责 |
| --- | --- |
| 教务规划子智能体 | 课程、成绩、课表、教学班和选课查询 |
| 学习教练子智能体 | 根据课程、考试时间和学习目标组织复习建议 |
| 知识检索子智能体 | 从教材和图书馆书籍知识库检索学习内容 |
| 校园事务子智能体 | 请假、资助、宿舍、公告等授权范围内查询 |
| 问题反馈子智能体 | 收集系统问题信息，形成管理员转 IT 的处理线索 |

后续可扩展就业导师、心理伙伴、安全卫士等子智能体，并支持串联、并行和层级协作。当前无外部 LLM 配置时，使用规则路由、本地检索和权限范围内业务查询保证演示可用。

## 5. 知识检索智能体

问答作为多智能体中的知识检索子智能体，不再作为独立“校园智能助手”板块。当前以《三国演义》为示范知识库。回答格式必须先展示来源书籍，再进行语言整理：

> 本回答参考《三国演义》相关内容。结合检索片段整理：……

生产环境可接入向量数据库、Embedding 和 LLM；没有外部服务时使用本地 `data/faiss/sanguo_chunks.pkl` 演示检索。知识库接口支持后续上传 `.txt`、`.pdf`、`.docx` 教材或图书馆书籍，上传者为管理员、教务管理员、教师或档案管理员，学生只读。每本书应记录书名、来源文件、上传人、上传时间、解析状态、章节和片段数。

## 6. 课程与成绩编排

课程设计参考成绩单样例，核心字段包括学号、姓名、专业、学期、课程名称、课程性质、学分、成绩和 GPA。系统将课程目录、学期、教学班、选课、成绩和成绩审核分开建模，但在前端以连续教务工作流呈现。

教师可在授权范围内发布课程、创建教学班，维护容量、选课开放时间、截止时间、课表 JSON 和状态；可修改本人教学班并关闭不再使用的教学班。教务管理员可跨学院维护并审核。Excel 仅作为学生、课程、班级、教学班等业务模块上传时支持的 `.xlsx` 格式，不作为独立业务模块或导航入口。

## 7. 系统管理员与 IT 问题闭环

系统管理员登录后进入独立的管理模块页，只保留“教师模块”和“学生模块”，不显示通用工作台。管理员从对应模块进入系统功能和问题总览，接收学生/教师提交的登录失败、页面报错、数据异常、权限问题等反馈。反馈包含模块、问题描述、发生时间、复现步骤、优先级、附件和当前处理状态；系统管理员负责分类、补充信息、转交 IT 维护人员并回填处理结果。学生和教师只能查看自己提交的问题，管理员可查看全校问题。

## 8. 演示数据与导入

界面演示表格默认只呈现三组数据，避免大量虚构数据干扰验收。正式环境不删除真实数据；导入采用预览、校验、确认三步流程，并记录导入批次和错误行。Excel 模板入口放在对应学生、课程、班级或教学班页面内。

## 9. 安全与运维

密码使用 bcrypt，登录令牌使用短期 JWT Cookie，学生会话单独校验有效期。所有业务 API 通过服务端角色依赖授权，严禁信任客户端传入的主体编号。书籍上传限制扩展名和文件名，生产环境还需增加大小、病毒扫描、内容解析和对象存储策略。开发环境支持 SQLite，生产环境可使用 MySQL；RAG 服务可接入 Milvus。

## 10. 当前阶段验收标准

- 学生、教师、管理员使用同一登录表单，依据账号自动进入对应界面。
- 登录背景使用校园照片并适度虚化，校徽和校训在登录页与工作台可见。
- 工作台出现“山河智能中枢”，可看到主智能体和至少五个子智能体。
- 输入“三国演义”相关问题时，回答先显示书籍来源，再显示检索整理内容。
- 管理员/教师可看到知识库上传入口，学生看不到上传控件。
- 客户端侧栏无接口文档、无独立 Excel 模块。
- 教师可打开课程与教学班入口，能够发布课程和维护本人教学班。
- 演示表格每个视图默认最多显示三组数据。
